# ===========================================
# MarkItUp - Environment Configuration
# ===========================================

# Node Environment
NODE_ENV=development

# Server Configuration
PORT=3000
HOSTNAME=localhost

# ===========================================
# Authentication & Security
# ===========================================

# JWT Secret for session tokens (REQUIRED for production)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-at-least-32-characters-long-change-in-production

# Encryption key for sensitive data like API keys (REQUIRED for production)
# Must be exactly 32 bytes
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY=your-encryption-key-exactly-32-bytes-change-in-production

# ===========================================
# Database
# ===========================================

# SQLite database file path (relative to project root)
# For production, consider PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:5432/markitup
DATABASE_URL=file:./markitup.db

# ===========================================
# AI Provider API Keys (Optional)
# ===========================================

# OpenAI
OPENAI_API_KEY=

# Anthropic Claude
ANTHROPIC_API_KEY=

# Google Gemini
GOOGLE_API_KEY=

# Ollama (Local AI - No API key needed)
OLLAMA_BASE_URL=http://localhost:11434

# ===========================================
# Email Configuration (Optional - for email verification)
# ===========================================

# SMTP Settings
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=
FROM_EMAIL=noreply@markitup.app

# ===========================================
# OAuth Providers (Optional)
# ===========================================

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ===========================================
# Application Settings
# ===========================================

# Public application URL (for OAuth callbacks, email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Feature Flags
ENABLE_ANALYTICS=true
ENABLE_TELEMETRY=false

# ===========================================
# Production Settings
# ===========================================

# For production deployment:
# 1. Change NODE_ENV to 'production'
# 2. Generate new JWT_SECRET and ENCRYPTION_KEY
# 3. Use PostgreSQL for DATABASE_URL
# 4. Configure SMTP for email
# 5. Set proper NEXT_PUBLIC_APP_URL
# 6. Enable HTTPS
